{
  "name": "smart-factory",
  "version": "1.0.0",
  "description": "Smart Factory Security System",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build step needed'",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
